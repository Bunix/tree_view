/**
 * to change the active menu when clicked
 **/
$(document).ready(function () {

    $('.active_with_sidebar_builder').addClass('active');

    $('#content').remove();

    $('#tabContent').remove();

});